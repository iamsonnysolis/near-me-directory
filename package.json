{
  "name": "near-me-directory",
  "version": "1.0.0",
  "type": "module",
  "description": "Generic, white-labelable near-me directory template (Astro + D1 + Tailwind)",
  "scripts": {
    "dev": "astro dev --host",
    "build": "astro build",
    "preview": "astro preview",
    "preview-deploy": "npm run build && wrangler pages deploy dist --project-name=near-me-directory --branch=preview",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=near-me-directory --branch=main"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.9",
    "@astrojs/cloudflare": "^12.0.0",
    "@astrojs/tailwind": "^5.1.5",
    "@lucide/astro": "^1.18.0",
    "astro": "^5.0.0",
    "dotenv": "^16.4.5",
    "tailwindcss": "^3.4.19",
    "typescript": "^5.9.3",
    "@cloudflare/workers-types": "^4.20250101.0"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "wrangler": "^4.100.0"
  }
}
